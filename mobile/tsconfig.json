{
  "extends": "expo/tsconfig.base",
  "compilerOptions": {
    "strict": true
  },
  // Tests are type-checked by tsconfig.test.json instead — they import with a
  // .ts extension, which node needs and the app project must not allow.
  "exclude": ["src/**/*.test.ts"]
}
